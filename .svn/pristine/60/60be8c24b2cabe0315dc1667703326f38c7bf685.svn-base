
angular.module('starter.controllers', [])
  .controller('tabCtrl', function($scope, $state, HttpService,customerHost,Util) {
    $scope.toTab = function(page){
    	$state.go('tab.' + page)
    };
  });
